remote_theme: pages-themes/minima
plugins:
- jekyll-remote-theme # add this line to the plugins list if you already have one

 # Defaults
 defaults:
   # _posts
   - scope:
       path: ""
       type: posts
     values:
       layout: single
       author_profile: true
       read_time: true
       comments: true
       share: true
       related: true
       mathjax: true